<!DOCTYPE html>
<html>
  <head>
    <title>ShippingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <%= link_to 'Registrar transportadora', new_carrier_path %>
    <%= link_to 'Registrar veículo', new_vehicle_path %>
    <%= yield %> 
    <%= alert %> 
    <%= notice %> 
  </body>
</html>
